# ROM dumps and chip images are binary — never touch line endings
*.bin binary
*.sie binary
*.xdf -text

# OpenGK reference mirror — keep verbatim, these are upstream artifacts
*.adx -text
*.wiki -text
